import 'package:flutter/material.dart';

class ColorItemPicker extends StatelessWidget {
  const ColorItemPicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircleAvatar(
      backgroundColor: Colors.blue,
      radius: 38,
    );
  }
}
